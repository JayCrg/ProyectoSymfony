{% extends 'base.html.twig' %}

{% block title %}Asignaturas{% endblock %}

{% block body %}
    {{ include('partials/navigation.html.twig') }}

    <h1>Asignaturas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ asignatura.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ asignatura.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_asignaturas_index') }}" class="btn btn-warning">Back to list</a>

    <a href="{{ path('app_asignaturas_edit', {'id': asignatura.id}) }}" class="btn btn-success">Edit</a>

    {{ include('asignaturas/_delete_form.html.twig') }}
{% endblock %}
