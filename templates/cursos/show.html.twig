{% extends 'base.html.twig' %}

{% block title %}Cursos{% endblock %}

{% block body %}
    {{ include('partials/navigation.html.twig') }}

    <h1>Cursos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ curso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ curso.nombre }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ curso.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cursos_index') }}"class="btn btn-warning">back to list</a>

    <a href="{{ path('app_cursos_edit', {'id': curso.id}) }}"class="btn btn-success">edit</a>

    {{ include('cursos/_delete_form.html.twig') }}
{% endblock %}
