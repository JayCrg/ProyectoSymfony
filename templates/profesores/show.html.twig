{% extends 'base.html.twig' %}

{% block title %}Profesores{% endblock %}

{% block body %}
    {{ include('partials/navigation.html.twig') }}

    <h1>Profesores</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profesore.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ profesore.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ profesore.apellidos }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ profesore.telefono }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profesores') }}"class="btn btn-warning">Back to list</a>

    <a href="{{ path('app_profesores_edit', {'id': profesore.id}) }}"class="btn btn-success">Edit</a>

    {{ include('profesores/_delete_form.html.twig') }}
{% endblock %}
